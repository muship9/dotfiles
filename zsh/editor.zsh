# ================================================================
# Editor Configuration
# ================================================================

# Default editor setup
export VISUAL="nvim"
export EDITOR="nvim"
export GIT_EDITOR="nvim"

# Key bindings
bindkey '^f' forward-word   # ctrl+F: 単語単位で前進
bindkey '^b' backward-word  # ctrl+B: 単語単位で後退
